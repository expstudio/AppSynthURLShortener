var path = require('path');
var rootPath = path.normalize(__dirname + '/../');

module.exports = {
  development: {
    address: 'https://app.tinyapp.biz/#'
  },
  production: {
    address: 'https://app.tinyapp.biz/#'
  }
}